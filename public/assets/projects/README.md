# Project Photography

Drop verified, permission-cleared project photographs here.

Not wired into any component yet — per instruction, exact real-photo
placement will be decided once all media is collected and each
photograph's project identity is explicitly confirmed. Do not assume or
guess which photo belongs to which project.

Suggested naming once ready: `<project-slug>-01.jpg`,
`<project-slug>-02.jpg`, etc., matching the `slug` values in
`src/data/projects.js` (currently `kr-puram-residence`,
`old-airport-road-residence`).
