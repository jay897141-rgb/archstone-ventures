# Videos

Drop optimized video assets here (e.g. an optional hero background
video) once available.

Not wired into any component yet — the current Hero uses an original
illustrative artwork, no video. Per the brief, no autoplay video should
be added unless a genuinely suitable, optimized asset exists; adding one
will need a small Hero.jsx change plus a mobile-fallback/poster-image
decision, which is worth doing as its own deliberate step once a real
file is here to test against, rather than pre-building against nothing.
