# Netlify Configuration for Archstone Ventures
# SPA (Single Page Application) redirect for React Router

[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect: send all non-file requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
